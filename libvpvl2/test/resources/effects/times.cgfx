float SDTime : TIME < bool SyncInEditMode = false; > = float(0.1);
float SDElapsed : ELAPSEDTIME < bool SyncInEditMode = false; > = float(0.2);
float SETime : TIME < bool SyncInEditMode = true; > = float(0.3);
float SEElapsed : ELAPSEDTIME < bool SyncInEditMode = true; > = float(0.4);

